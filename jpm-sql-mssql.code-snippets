{
  "MSSQL Script Bootstrap": {
    "prefix": "nums",
    "scope": "sql",
    "description": "Bootstrap a new MSSQL script with header, guards, function, CTEs, and change log",
    "body": [
      "-- ============================================================",
      "-- Script:  ${1:script_name}.sql",
      "-- Author:  Joseph P. Merten",
      "-- Date:    $CURRENT_YEAR-$CURRENT_MONTH-$CURRENT_DATE",
      "-- Purpose: ${2:Describe purpose here}",
      "-- ============================================================",
      "",
      "-- ─── Configuration ──────────────────────────────────────────",
      "-- Target DB:  ${3:database_name}",
      "-- Schema:     ${4:dbo}",
      "-- Environment: ${5:dev|test|uat|prod}",
      "",
      "USE [${3:database_name}];",
      "GO",
      "SET NOCOUNT ON;",
      "SET XACT_ABORT ON;",
      "GO",
      "",
      "-- ─── Safety Guards ──────────────────────────────────────────",
      "BEGIN TRANSACTION;",
      "-- ROLLBACK TRANSACTION;  -- Uncomment to test without committing",
      "",
      "-- ─── Function Definition ────────────────────────────────────",
      "IF OBJECT_ID('${4:dbo}.${6:function_name}', 'FN') IS NOT NULL",
      "    DROP FUNCTION [${4:dbo}].[${6:function_name}];",
      "GO",
      "",
      "CREATE FUNCTION [${4:dbo}].[${6:function_name}]",
      "(",
      "    @${7:param_name} ${8:param_type}",
      ")",
      "RETURNS ${9:return_type}",
      "AS",
      "BEGIN",
      "    -- ── Declare Variables ─────────────────────────────────",
      "    DECLARE @${10:v_variable} ${11:variable_type};",
      "",
      "    -- ── Function Logic ────────────────────────────────────",
      "    ${12:-- your logic here}",
      "",
      "    RETURN @${10:v_variable};",
      "END;",
      "GO",
      "",
      "-- Grant execute to app role",
      "GRANT EXECUTE ON [${4:dbo}].[${6:function_name}] TO [${13:app_role}];",
      "GO",
      "",
      "-- ─── Variables ───────────────────────────────────────────────",
      "DECLARE @${14:variable_name} ${15:variable_type} = ${16:value};",
      "",
      "-- ─── CTEs / Main Query ───────────────────────────────────────",
      "WITH ${17:cte_name} AS (",
      "    SELECT ${18:column_list}",
      "    FROM   [${3:database_name}].[${4:dbo}].[${19:table_name}]",
      "    WHERE  ${20:1=1}",
      ")",
      "SELECT  ${21:*}",
      "FROM    ${17:cte_name}",
      "ORDER BY ${22:1};",
      "",
      "-- ─── Cleanup / Commit ───────────────────────────────────────",
      "-- COMMIT TRANSACTION;  -- Uncomment when ready to execute",
      "",
      "-- ─── Change Log ─────────────────────────────────────────────",
      "-- $CURRENT_YEAR-$CURRENT_MONTH-$CURRENT_DATE | Joseph P. Merten | Initial version"
    ]
  },

  "MSSQL Window Functions - Ranking": {
    "prefix": "numsw1",
    "scope": "sql",
    "description": "Ranking window functions - ROW_NUMBER, RANK, DENSE_RANK, NTILE",
    "body": [
      "-- ============================================================",
      "-- Script:  ${1:script_name}.sql",
      "-- Author:  Joseph P. Merten",
      "-- Date:    $CURRENT_YEAR-$CURRENT_MONTH-$CURRENT_DATE",
      "-- Purpose: Ranking window functions against [${3:schema_name}].[${4:table_name}]",
      "-- ============================================================",
      "",
      "USE [${2:database_name}];",
      "GO",
      "",
      "-- ─── Context ─────────────────────────────────────────────────",
      "-- Partition Key : ${5:partition_col}   (e.g. department, region, customer_id)",
      "-- Order Key     : ${6:order_col}       (e.g. sale_date, created_dt, amount)",
      "-- Measure       : ${7:measure_col}     (e.g. sales_amount, qty, revenue)",
      "",
      "-- ─── 1. ROW_NUMBER ───────────────────────────────────────────",
      "-- Unique sequential rank per partition - no ties ever",
      "-- Use for: deduplication, picking exactly one row per group",
      "SELECT",
      "    ${5:partition_col},",
      "    ${6:order_col},",
      "    ${7:measure_col},",
      "    ROW_NUMBER() OVER (",
      "        PARTITION BY ${5:partition_col}",
      "        ORDER BY     ${6:order_col} DESC",
      "    ) AS row_num",
      "FROM [${3:schema_name}].[${4:table_name}]",
      "ORDER BY ${5:partition_col}, row_num;",
      "",
      "",
      "-- ─── 2. RANK ─────────────────────────────────────────────────",
      "-- Same rank for ties, skips numbers after (1, 2, 2, 4)",
      "-- Use for: leaderboards where gaps after ties matter",
      "SELECT",
      "    ${5:partition_col},",
      "    ${6:order_col},",
      "    ${7:measure_col},",
      "    RANK() OVER (",
      "        PARTITION BY ${5:partition_col}",
      "        ORDER BY     ${7:measure_col} DESC",
      "    ) AS rnk",
      "FROM [${3:schema_name}].[${4:table_name}]",
      "ORDER BY ${5:partition_col}, rnk;",
      "",
      "",
      "-- ─── 3. DENSE_RANK ───────────────────────────────────────────",
      "-- Same rank for ties, no gaps (1, 2, 2, 3)",
      "-- Use for: tier grouping where gaps would be confusing",
      "SELECT",
      "    ${5:partition_col},",
      "    ${6:order_col},",
      "    ${7:measure_col},",
      "    DENSE_RANK() OVER (",
      "        PARTITION BY ${5:partition_col}",
      "        ORDER BY     ${7:measure_col} DESC",
      "    ) AS dense_rnk",
      "FROM [${3:schema_name}].[${4:table_name}]",
      "ORDER BY ${5:partition_col}, dense_rnk;",
      "",
      "",
      "-- ─── 4. RANK vs DENSE_RANK Side by Side ─────────────────────",
      "-- Compare the two to see the difference clearly",
      "SELECT",
      "    ${5:partition_col},",
      "    ${7:measure_col},",
      "    RANK() OVER (",
      "        PARTITION BY ${5:partition_col}",
      "        ORDER BY     ${7:measure_col} DESC",
      "    ) AS rnk,",
      "    DENSE_RANK() OVER (",
      "        PARTITION BY ${5:partition_col}",
      "        ORDER BY     ${7:measure_col} DESC",
      "    ) AS dense_rnk",
      "FROM [${3:schema_name}].[${4:table_name}]",
      "ORDER BY ${5:partition_col}, rnk;",
      "",
      "",
      "-- ─── 5. NTILE ────────────────────────────────────────────────",
      "-- Divides rows into N equal buckets",
      "-- Use for: quartiles, deciles, segmentation",
      "SELECT",
      "    ${5:partition_col},",
      "    ${6:order_col},",
      "    ${7:measure_col},",
      "    NTILE(4) OVER (",
      "        PARTITION BY ${5:partition_col}",
      "        ORDER BY     ${7:measure_col} DESC",
      "    ) AS quartile,",
      "    NTILE(10) OVER (",
      "        PARTITION BY ${5:partition_col}",
      "        ORDER BY     ${7:measure_col} DESC",
      "    ) AS decile",
      "FROM [${3:schema_name}].[${4:table_name}]",
      "ORDER BY ${5:partition_col}, quartile;",
      "",
      "",
      "-- ─── 6. PRACTICAL PATTERN - Top N Per Group ──────────────────",
      "-- Most common real-world ranking use case",
      "-- Use ROW_NUMBER in a subquery to filter cleanly",
      "SELECT *",
      "FROM (",
      "    SELECT",
      "        ${5:partition_col},",
      "        ${6:order_col},",
      "        ${7:measure_col},",
      "        ROW_NUMBER() OVER (",
      "            PARTITION BY ${5:partition_col}",
      "            ORDER BY     ${7:measure_col} DESC",
      "        ) AS row_num",
      "    FROM [${3:schema_name}].[${4:table_name}]",
      ") AS ranked",
      "WHERE row_num <= ${8:top_n}",
      "ORDER BY ${5:partition_col}, row_num;",
      "",
      "-- ─── Change Log ─────────────────────────────────────────────",
      "-- $CURRENT_YEAR-$CURRENT_MONTH-$CURRENT_DATE | Joseph P. Merten | Initial version",
      "${0}"
    ]
  },

  "MSSQL Window Functions - Aggregate": {
    "prefix": "numsw2",
    "scope": "sql",
    "description": "Aggregate window functions - SUM, AVG, COUNT, MIN, MAX with running totals and moving averages",
    "body": [
      "-- ============================================================",
      "-- Script:  ${1:script_name}.sql",
      "-- Author:  Joseph P. Merten",
      "-- Date:    $CURRENT_YEAR-$CURRENT_MONTH-$CURRENT_DATE",
      "-- Purpose: Aggregate window functions against [${3:schema_name}].[${4:table_name}]",
      "-- ============================================================",
      "",
      "USE [${2:database_name}];",
      "GO",
      "",
      "-- ─── Context ─────────────────────────────────────────────────",
      "-- Partition Key : ${5:partition_col}   (e.g. department, region, customer_id)",
      "-- Order Key     : ${6:order_col}       (e.g. sale_date, created_dt, amount)",
      "-- Measure       : ${7:measure_col}     (e.g. sales_amount, qty, revenue)",
      "",
      "-- ─── 1. RUNNING TOTAL ────────────────────────────────────────",
      "-- Cumulative SUM from first row to current row",
      "-- Use for: year-to-date totals, cumulative revenue",
      "SELECT",
      "    ${5:partition_col},",
      "    ${6:order_col},",
      "    ${7:measure_col},",
      "    SUM(${7:measure_col}) OVER (",
      "        PARTITION BY ${5:partition_col}",
      "        ORDER BY     ${6:order_col}",
      "        ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW",
      "    ) AS running_total",
      "FROM [${3:schema_name}].[${4:table_name}]",
      "ORDER BY ${5:partition_col}, ${6:order_col};",
      "",
      "",
      "-- ─── 2. MOVING AVERAGE ───────────────────────────────────────",
      "-- Average over a sliding window of N periods",
      "-- Use for: smoothing trends, removing noise",
      "SELECT",
      "    ${5:partition_col},",
      "    ${6:order_col},",
      "    ${7:measure_col},",
      "    AVG(CAST(${7:measure_col} AS FLOAT)) OVER (",
      "        PARTITION BY ${5:partition_col}",
      "        ORDER BY     ${6:order_col}",
      "        ROWS BETWEEN 2 PRECEDING AND CURRENT ROW",
      "    ) AS moving_avg_3,",
      "    AVG(CAST(${7:measure_col} AS FLOAT)) OVER (",
      "        PARTITION BY ${5:partition_col}",
      "        ORDER BY     ${6:order_col}",
      "        ROWS BETWEEN 6 PRECEDING AND CURRENT ROW",
      "    ) AS moving_avg_7",
      "FROM [${3:schema_name}].[${4:table_name}]",
      "ORDER BY ${5:partition_col}, ${6:order_col};",
      "",
      "",
      "-- ─── 3. PARTITION TOTAL vs RUNNING TOTAL ────────────────────",
      "-- Full partition total (no ORDER BY = entire partition)",
      "-- Use for: percent of total calculations",
      "SELECT",
      "    ${5:partition_col},",
      "    ${6:order_col},",
      "    ${7:measure_col},",
      "    SUM(${7:measure_col}) OVER (",
      "        PARTITION BY ${5:partition_col}",
      "    ) AS partition_total,",
      "    SUM(${7:measure_col}) OVER (",
      "        PARTITION BY ${5:partition_col}",
      "        ORDER BY     ${6:order_col}",
      "        ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW",
      "    ) AS running_total,",
      "    ROUND(",
      "        100.0 * ${7:measure_col} /",
      "        NULLIF(SUM(${7:measure_col}) OVER (PARTITION BY ${5:partition_col}), 0)",
      "    , 2) AS pct_of_total",
      "FROM [${3:schema_name}].[${4:table_name}]",
      "ORDER BY ${5:partition_col}, ${6:order_col};",
      "",
      "",
      "-- ─── 4. ROLLING MIN / MAX ────────────────────────────────────",
      "-- Min and Max over a sliding window",
      "-- Use for: detecting peaks, floors, Bollinger-style bands",
      "SELECT",
      "    ${5:partition_col},",
      "    ${6:order_col},",
      "    ${7:measure_col},",
      "    MIN(${7:measure_col}) OVER (",
      "        PARTITION BY ${5:partition_col}",
      "        ORDER BY     ${6:order_col}",
      "        ROWS BETWEEN 6 PRECEDING AND CURRENT ROW",
      "    ) AS rolling_min_7,",
      "    MAX(${7:measure_col}) OVER (",
      "        PARTITION BY ${5:partition_col}",
      "        ORDER BY     ${6:order_col}",
      "        ROWS BETWEEN 6 PRECEDING AND CURRENT ROW",
      "    ) AS rolling_max_7",
      "FROM [${3:schema_name}].[${4:table_name}]",
      "ORDER BY ${5:partition_col}, ${6:order_col};",
      "",
      "",
      "-- ─── 5. RUNNING COUNT ────────────────────────────────────────",
      "-- Count of rows from start of partition to current row",
      "-- Use for: tracking event counts over time per entity",
      "SELECT",
      "    ${5:partition_col},",
      "    ${6:order_col},",
      "    ${7:measure_col},",
      "    COUNT(*) OVER (",
      "        PARTITION BY ${5:partition_col}",
      "        ORDER BY     ${6:order_col}",
      "        ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW",
      "    ) AS running_count",
      "FROM [${3:schema_name}].[${4:table_name}]",
      "ORDER BY ${5:partition_col}, ${6:order_col};",
      "",
      "-- ─── Change Log ─────────────────────────────────────────────",
      "-- $CURRENT_YEAR-$CURRENT_MONTH-$CURRENT_DATE | Joseph P. Merten | Initial version",
      "${0}"
    ]
  },

  "MSSQL Window Functions - Value/Offset": {
    "prefix": "numsw3",
    "scope": "sql",
    "description": "Value/Offset window functions - LAG, LEAD, FIRST_VALUE, LAST_VALUE, NTH_VALUE",
    "body": [
      "-- ============================================================",
      "-- Script:  ${1:script_name}.sql",
      "-- Author:  Joseph P. Merten",
      "-- Date:    $CURRENT_YEAR-$CURRENT_MONTH-$CURRENT_DATE",
      "-- Purpose: Value/Offset window functions against [${3:schema_name}].[${4:table_name}]",
      "-- ============================================================",
      "",
      "USE [${2:database_name}];",
      "GO",
      "",
      "-- ─── Context ─────────────────────────────────────────────────",
      "-- Partition Key : ${5:partition_col}   (e.g. department, region, customer_id)",
      "-- Order Key     : ${6:order_col}       (e.g. sale_date, created_dt, amount)",
      "-- Measure       : ${7:measure_col}     (e.g. sales_amount, qty, revenue)",
      "",
      "-- ─── 1. LAG ──────────────────────────────────────────────────",
      "-- Access value from N rows BEFORE current row",
      "-- Use for: period-over-period comparison without a self join",
      "-- 3rd arg = default when no previous row exists (avoids NULL)",
      "SELECT",
      "    ${5:partition_col},",
      "    ${6:order_col},",
      "    ${7:measure_col},",
      "    LAG(${7:measure_col}, 1, 0) OVER (",
      "        PARTITION BY ${5:partition_col}",
      "        ORDER BY     ${6:order_col}",
      "    ) AS prev_value,",
      "    ${7:measure_col} - LAG(${7:measure_col}, 1, 0) OVER (",
      "        PARTITION BY ${5:partition_col}",
      "        ORDER BY     ${6:order_col}",
      "    ) AS period_change",
      "FROM [${3:schema_name}].[${4:table_name}]",
      "ORDER BY ${5:partition_col}, ${6:order_col};",
      "",
      "",
      "-- ─── 2. LEAD ─────────────────────────────────────────────────",
      "-- Access value from N rows AFTER current row",
      "-- Use for: forecasting comparison, next event lookup",
      "SELECT",
      "    ${5:partition_col},",
      "    ${6:order_col},",
      "    ${7:measure_col},",
      "    LEAD(${7:measure_col}, 1, 0) OVER (",
      "        PARTITION BY ${5:partition_col}",
      "        ORDER BY     ${6:order_col}",
      "    ) AS next_value,",
      "    LEAD(${7:measure_col}, 1, 0) OVER (",
      "        PARTITION BY ${5:partition_col}",
      "        ORDER BY     ${6:order_col}",
      "    ) - ${7:measure_col} AS next_period_change",
      "FROM [${3:schema_name}].[${4:table_name}]",
      "ORDER BY ${5:partition_col}, ${6:order_col};",
      "",
      "",
      "-- ─── 3. LAG & LEAD Together ──────────────────────────────────",
      "-- Compare previous, current, and next in one query",
      "SELECT",
      "    ${5:partition_col},",
      "    ${6:order_col},",
      "    LAG(${7:measure_col},  1, 0) OVER (",
      "        PARTITION BY ${5:partition_col} ORDER BY ${6:order_col}",
      "    ) AS prev_value,",
      "    ${7:measure_col}              AS current_value,",
      "    LEAD(${7:measure_col}, 1, 0) OVER (",
      "        PARTITION BY ${5:partition_col} ORDER BY ${6:order_col}",
      "    ) AS next_value",
      "FROM [${3:schema_name}].[${4:table_name}]",
      "ORDER BY ${5:partition_col}, ${6:order_col};",
      "",
      "",
      "-- ─── 4. FIRST_VALUE ──────────────────────────────────────────",
      "-- First value in the partition window",
      "-- Use for: compare each row to the baseline/starting value",
      "SELECT",
      "    ${5:partition_col},",
      "    ${6:order_col},",
      "    ${7:measure_col},",
      "    FIRST_VALUE(${7:measure_col}) OVER (",
      "        PARTITION BY ${5:partition_col}",
      "        ORDER BY     ${6:order_col}",
      "        ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING",
      "    ) AS first_val,",
      "    ${7:measure_col} - FIRST_VALUE(${7:measure_col}) OVER (",
      "        PARTITION BY ${5:partition_col}",
      "        ORDER BY     ${6:order_col}",
      "        ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING",
      "    ) AS change_from_first",
      "FROM [${3:schema_name}].[${4:table_name}]",
      "ORDER BY ${5:partition_col}, ${6:order_col};",
      "",
      "",
      "-- ─── 5. LAST_VALUE ───────────────────────────────────────────",
      "-- Last value in the partition window",
      "-- CRITICAL: always include explicit frame clause or LAST_VALUE",
      "-- defaults to CURRENT ROW and returns the current value instead",
      "SELECT",
      "    ${5:partition_col},",
      "    ${6:order_col},",
      "    ${7:measure_col},",
      "    LAST_VALUE(${7:measure_col}) OVER (",
      "        PARTITION BY ${5:partition_col}",
      "        ORDER BY     ${6:order_col}",
      "        ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING",
      "    ) AS last_val",
      "FROM [${3:schema_name}].[${4:table_name}]",
      "ORDER BY ${5:partition_col}, ${6:order_col};",
      "",
      "",
      "-- ─── 6. NTH_VALUE ────────────────────────────────────────────",
      "-- Nth value in the partition window",
      "-- Use for: compare each row to 2nd place, 3rd place etc.",
      "SELECT",
      "    ${5:partition_col},",
      "    ${6:order_col},",
      "    ${7:measure_col},",
      "    NTH_VALUE(${7:measure_col}, ${8:2}) OVER (",
      "        PARTITION BY ${5:partition_col}",
      "        ORDER BY     ${6:order_col} DESC",
      "        ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING",
      "    ) AS nth_val",
      "FROM [${3:schema_name}].[${4:table_name}]",
      "ORDER BY ${5:partition_col}, ${6:order_col};",
      "",
      "-- ─── Change Log ─────────────────────────────────────────────",
      "-- $CURRENT_YEAR-$CURRENT_MONTH-$CURRENT_DATE | Joseph P. Merten | Initial version",
      "${0}"
    ]
  },

  "MSSQL Window Functions - Distribution": {
    "prefix": "numsw4",
    "scope": "sql",
    "description": "Distribution window functions - PERCENT_RANK, CUME_DIST, PERCENTILE_CONT, PERCENTILE_DISC",
    "body": [
      "-- ============================================================",
      "-- Script:  ${1:script_name}.sql",
      "-- Author:  Joseph P. Merten",
      "-- Date:    $CURRENT_YEAR-$CURRENT_MONTH-$CURRENT_DATE",
      "-- Purpose: Distribution window functions against [${3:schema_name}].[${4:table_name}]",
      "-- ============================================================",
      "",
      "USE [${2:database_name}];",
      "GO",
      "",
      "-- ─── Context ─────────────────────────────────────────────────",
      "-- Partition Key : ${5:partition_col}   (e.g. department, region, customer_id)",
      "-- Order Key     : ${6:order_col}       (e.g. sale_date, created_dt, amount)",
      "-- Measure       : ${7:measure_col}     (e.g. sales_amount, qty, revenue)",
      "",
      "-- ─── 1. PERCENT_RANK ─────────────────────────────────────────",
      "-- Relative rank of row as 0.0 to 1.0",
      "-- First row is always 0.0, last row is always 1.0",
      "-- Formula: (rank - 1) / (total rows - 1)",
      "-- Use for: scoring where you want 0 at the bottom",
      "SELECT",
      "    ${5:partition_col},",
      "    ${6:order_col},",
      "    ${7:measure_col},",
      "    CAST(ROUND(PERCENT_RANK() OVER (",
      "        PARTITION BY ${5:partition_col}",
      "        ORDER BY     ${7:measure_col}",
      "    ), 4) AS DECIMAL(18,4)) AS pct_rank",
      "FROM [${3:schema_name}].[${4:table_name}]",
      "ORDER BY ${5:partition_col}, pct_rank;",
      "",
      "",
      "-- ─── 2. CUME_DIST ────────────────────────────────────────────",
      "-- Cumulative distribution - what fraction of rows are at or",
      "-- below the current value. Always > 0, last row always 1.0",
      "-- Formula: rank / total rows",
      "-- Use for: 'what % of customers spend at or below this amount'",
      "SELECT",
      "    ${5:partition_col},",
      "    ${6:order_col},",
      "    ${7:measure_col},",
      "    CAST(ROUND(CUME_DIST() OVER (",
      "        PARTITION BY ${5:partition_col}",
      "        ORDER BY     ${7:measure_col}",
      "    ), 4) AS DECIMAL(18,4)) AS cume_dist",
      "FROM [${3:schema_name}].[${4:table_name}]",
      "ORDER BY ${5:partition_col}, cume_dist;",
      "",
      "",
      "-- ─── 3. PERCENT_RANK vs CUME_DIST Side by Side ──────────────",
      "-- Compare to see the difference clearly",
      "-- PERCENT_RANK: first=0, excludes current from denominator",
      "-- CUME_DIST:    first>0, includes current in denominator",
      "SELECT",
      "    ${5:partition_col},",
      "    ${7:measure_col},",
      "    CAST(ROUND(PERCENT_RANK() OVER (",
      "        PARTITION BY ${5:partition_col}",
      "        ORDER BY     ${7:measure_col}",
      "    ), 4) AS DECIMAL(18,4)) AS pct_rank,",
      "    CAST(ROUND(CUME_DIST() OVER (",
      "        PARTITION BY ${5:partition_col}",
      "        ORDER BY     ${7:measure_col}",
      "    ), 4) AS DECIMAL(18,4)) AS cume_dist",
      "FROM [${3:schema_name}].[${4:table_name}]",
      "ORDER BY ${5:partition_col}, pct_rank;",
      "",
      "",
      "-- ─── 4. PERCENTILE_CONT ──────────────────────────────────────",
      "-- Continuous percentile - interpolates between values",
      "-- Returns a value that may not exist in the actual data",
      "-- Use for: median, p90, p95, p99 calculations",
      "-- NOTE: WITHIN GROUP is an ordered-set aggregate, not a",
      "--       standard window function - no PARTITION BY here",
      "SELECT",
      "    ${5:partition_col},",
      "    PERCENTILE_CONT(0.25) WITHIN GROUP (ORDER BY ${7:measure_col}) AS p25,",
      "    PERCENTILE_CONT(0.50) WITHIN GROUP (ORDER BY ${7:measure_col}) AS median,",
      "    PERCENTILE_CONT(0.75) WITHIN GROUP (ORDER BY ${7:measure_col}) AS p75,",
      "    PERCENTILE_CONT(0.90) WITHIN GROUP (ORDER BY ${7:measure_col}) AS p90,",
      "    PERCENTILE_CONT(0.95) WITHIN GROUP (ORDER BY ${7:measure_col}) AS p95,",
      "    PERCENTILE_CONT(0.99) WITHIN GROUP (ORDER BY ${7:measure_col}) AS p99",
      "FROM [${3:schema_name}].[${4:table_name}]",
      "GROUP BY ${5:partition_col}",
      "ORDER BY ${5:partition_col};",
      "",
      "",
      "-- ─── 5. PERCENTILE_DISC ──────────────────────────────────────",
      "-- Discrete percentile - returns actual value from the dataset",
      "-- Will never return an interpolated value",
      "-- Use for: when you need a real observed value at a percentile",
      "SELECT",
      "    ${5:partition_col},",
      "    PERCENTILE_DISC(0.25) WITHIN GROUP (ORDER BY ${7:measure_col}) AS p25,",
      "    PERCENTILE_DISC(0.50) WITHIN GROUP (ORDER BY ${7:measure_col}) AS median,",
      "    PERCENTILE_DISC(0.75) WITHIN GROUP (ORDER BY ${7:measure_col}) AS p75,",
      "    PERCENTILE_DISC(0.90) WITHIN GROUP (ORDER BY ${7:measure_col}) AS p90,",
      "    PERCENTILE_DISC(0.95) WITHIN GROUP (ORDER BY ${7:measure_col}) AS p95,",
      "    PERCENTILE_DISC(0.99) WITHIN GROUP (ORDER BY ${7:measure_col}) AS p99",
      "FROM [${3:schema_name}].[${4:table_name}]",
      "GROUP BY ${5:partition_col}",
      "ORDER BY ${5:partition_col};",
      "",
      "",
      "-- ─── 6. PERCENTILE_CONT vs PERCENTILE_DISC Side by Side ─────",
      "-- CONT interpolates (may not be in data)",
      "-- DISC returns nearest actual value from data",
      "SELECT",
      "    ${5:partition_col},",
      "    PERCENTILE_CONT(0.50) WITHIN GROUP (ORDER BY ${7:measure_col}) AS median_cont,",
      "    PERCENTILE_DISC(0.50) WITHIN GROUP (ORDER BY ${7:measure_col}) AS median_disc",
      "FROM [${3:schema_name}].[${4:table_name}]",
      "GROUP BY ${5:partition_col}",
      "ORDER BY ${5:partition_col};",
      "",
      "-- ─── Change Log ─────────────────────────────────────────────",
      "-- $CURRENT_YEAR-$CURRENT_MONTH-$CURRENT_DATE | Joseph P. Merten | Initial version",
      "${0}"
    ]
  },

  "MSSQL Window Functions - Deduplication": {
    "prefix": "numsw5",
    "scope": "sql",
    "description": "Deduplication patterns using ROW_NUMBER window function",
    "body": [
      "-- ============================================================",
      "-- Script:  ${1:script_name}.sql",
      "-- Author:  Joseph P. Merten",
      "-- Date:    $CURRENT_YEAR-$CURRENT_MONTH-$CURRENT_DATE",
      "-- Purpose: Deduplication of [${3:schema_name}].[${4:table_name}]",
      "-- ============================================================",
      "",
      "USE [${2:database_name}];",
      "GO",
      "",
      "-- ─── Safety Guards ──────────────────────────────────────────",
      "BEGIN TRANSACTION;",
      "-- ROLLBACK TRANSACTION;  -- Uncomment to test without committing",
      "",
      "-- ─── Context ─────────────────────────────────────────────────",
      "-- Table        : [${3:schema_name}].[${4:table_name}]",
      "-- ID Column    : ${5:id_col}         (unique row identifier, e.g. id, row_id)",
      "-- Dupe Column  : ${6:duplicate_col}  (column(s) that define a duplicate)",
      "-- Order Column : ${7:order_col}      (determines which duplicate to KEEP, e.g. created_dt keeps oldest)",
      "",
      "-- ─── 1. PREVIEW DUPLICATES ───────────────────────────────────",
      "-- Always run this first — confirm what will be deleted",
      "SELECT *",
      "FROM (",
      "    SELECT *,",
      "           ROW_NUMBER() OVER (",
      "               PARTITION BY ${6:duplicate_col}",
      "               ORDER BY     ${7:order_col}",
      "           ) AS rn",
      "    FROM [${3:schema_name}].[${4:table_name}]",
      ") t",
      "WHERE rn > 1",
      "ORDER BY ${6:duplicate_col}, ${7:order_col};",
      "",
      "",
      "-- ─── 2. DELETE via Subquery ──────────────────────────────────",
      "-- Standard approach - works in all SQL dialects",
      "DELETE FROM [${3:schema_name}].[${4:table_name}]",
      "WHERE ${5:id_col} IN (",
      "    SELECT ${5:id_col} FROM (",
      "        SELECT ${5:id_col},",
      "               ROW_NUMBER() OVER (",
      "                   PARTITION BY ${6:duplicate_col}",
      "                   ORDER BY     ${7:order_col}",
      "               ) AS rn",
      "        FROM [${3:schema_name}].[${4:table_name}]",
      "    ) t",
      "    WHERE rn > 1",
      ");",
      "",
      "",
      "-- ─── 3. DELETE via CTE ───────────────────────────────────────",
      "-- T-SQL superpower: DELETE directly from the CTE — no subquery needed",
      "WITH dupes AS (",
      "    SELECT ${5:id_col},",
      "           ROW_NUMBER() OVER (",
      "               PARTITION BY ${6:duplicate_col}",
      "               ORDER BY     ${7:order_col}",
      "           ) AS rn",
      "    FROM [${3:schema_name}].[${4:table_name}]",
      ")",
      "DELETE FROM dupes",
      "WHERE rn > 1;",
      "",
      "",
      "-- ─── 4. SAFE PATTERN - Backup First, Then Delete ─────────────",
      "-- Preserve rows to be removed before deleting",
      "SELECT *",
      "INTO [${3:schema_name}].[${4:table_name}_dupes_backup]",
      "FROM (",
      "    SELECT *,",
      "           ROW_NUMBER() OVER (",
      "               PARTITION BY ${6:duplicate_col}",
      "               ORDER BY     ${7:order_col}",
      "           ) AS rn",
      "    FROM [${3:schema_name}].[${4:table_name}]",
      ") t",
      "WHERE rn > 1;",
      "",
      "-- Review backup before proceeding:",
      "SELECT COUNT(*) AS rows_to_delete FROM [${3:schema_name}].[${4:table_name}_dupes_backup];",
      "",
      "-- Then delete using the backup as the source:",
      "DELETE FROM [${3:schema_name}].[${4:table_name}]",
      "WHERE ${5:id_col} IN (",
      "    SELECT ${5:id_col} FROM [${3:schema_name}].[${4:table_name}_dupes_backup]",
      ");",
      "",
      "",
      "-- ─── 5. VERIFY ───────────────────────────────────────────────",
      "-- Confirm no duplicates remain",
      "SELECT ${6:duplicate_col}, COUNT(*) AS cnt",
      "FROM   [${3:schema_name}].[${4:table_name}]",
      "GROUP BY ${6:duplicate_col}",
      "HAVING COUNT(*) > 1",
      "ORDER BY cnt DESC;",
      "",
      "-- COMMIT TRANSACTION;  -- Uncomment when satisfied with results",
      "",
      "-- ─── Change Log ─────────────────────────────────────────────",
      "-- $CURRENT_YEAR-$CURRENT_MONTH-$CURRENT_DATE | Joseph P. Merten | Initial version",
      "${0}"
    ]
  },

  "MSSQL Gap and Island - Date Streaks": {
    "prefix": "numsgi",
    "scope": "sql",
    "description": "Gap and Island pattern — find continuous date streaks and gaps per user",
    "body": [
      "-- ============================================================",
      "-- Script:  ${1:script_name}.sql",
      "-- Author:  Joseph P. Merten",
      "-- Date:    $CURRENT_YEAR-$CURRENT_MONTH-$CURRENT_DATE",
      "-- Purpose: Gap and Island — date streaks in [${2:schema_name}].[${3:table_name}]",
      "-- ============================================================",
      "",
      "-- ─── Pattern Overview ───────────────────────────────────────",
      "-- flagged      : LAG flags where a new island starts (prev day ≠ current day - 1)",
      "-- islands      : Running SUM of the flag assigns each consecutive run an island_id",
      "-- island_ranges: MIN/MAX per island_id → streak_start, streak_end, days_in_streak",
      "",
      "-- ─── Streaks ─────────────────────────────────────────────────",
      "WITH flagged AS (",
      "    SELECT",
      "        ${4:user_id_col},",
      "        ${5:date_col},",
      "        CASE",
      "            WHEN LAG(${5:date_col}) OVER (",
      "                     PARTITION BY ${4:user_id_col}",
      "                     ORDER BY     ${5:date_col}",
      "                 ) = DATEADD(day, -1, ${5:date_col})",
      "            THEN 0",
      "            ELSE 1",
      "        END AS is_new_island",
      "    FROM [${2:schema_name}].[${3:table_name}]",
      "),",
      "islands AS (",
      "    SELECT",
      "        ${4:user_id_col},",
      "        ${5:date_col},",
      "        SUM(is_new_island) OVER (",
      "            PARTITION BY ${4:user_id_col}",
      "            ORDER BY     ${5:date_col}",
      "            ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW",
      "        ) AS island_id",
      "    FROM flagged",
      "),",
      "island_ranges AS (",
      "    SELECT",
      "        ${4:user_id_col},",
      "        island_id,",
      "        MIN(${5:date_col})                                       AS streak_start,",
      "        MAX(${5:date_col})                                       AS streak_end,",
      "        DATEDIFF(day, MIN(${5:date_col}), MAX(${5:date_col})) + 1 AS days_in_streak",
      "    FROM islands",
      "    GROUP BY ${4:user_id_col}, island_id",
      ")",
      "SELECT *",
      "FROM   island_ranges",
      "ORDER BY ${4:user_id_col}, streak_start;",
      "",
      "",
      "-- ─── Gaps ─────────────────────────────────────────────────────",
      "-- Extends the same CTE chain with a gaps CTE using LEAD",
      "WITH flagged AS (",
      "    SELECT",
      "        ${4:user_id_col},",
      "        ${5:date_col},",
      "        CASE",
      "            WHEN LAG(${5:date_col}) OVER (",
      "                     PARTITION BY ${4:user_id_col}",
      "                     ORDER BY     ${5:date_col}",
      "                 ) = DATEADD(day, -1, ${5:date_col})",
      "            THEN 0",
      "            ELSE 1",
      "        END AS is_new_island",
      "    FROM [${2:schema_name}].[${3:table_name}]",
      "),",
      "islands AS (",
      "    SELECT",
      "        ${4:user_id_col},",
      "        ${5:date_col},",
      "        SUM(is_new_island) OVER (",
      "            PARTITION BY ${4:user_id_col}",
      "            ORDER BY     ${5:date_col}",
      "            ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW",
      "        ) AS island_id",
      "    FROM flagged",
      "),",
      "island_ranges AS (",
      "    SELECT",
      "        ${4:user_id_col},",
      "        island_id,",
      "        MIN(${5:date_col}) AS streak_start,",
      "        MAX(${5:date_col}) AS streak_end",
      "    FROM islands",
      "    GROUP BY ${4:user_id_col}, island_id",
      "),",
      "gaps AS (",
      "    SELECT",
      "        ${4:user_id_col},",
      "        streak_end                          AS gap_start,",
      "        LEAD(streak_start) OVER (",
      "            PARTITION BY ${4:user_id_col}",
      "            ORDER BY     streak_start",
      "        )                                   AS gap_end,",
      "        DATEDIFF(day, streak_end, LEAD(streak_start) OVER (",
      "            PARTITION BY ${4:user_id_col}",
      "            ORDER BY     streak_start",
      "        )) - 1                              AS gap_days",
      "    FROM island_ranges",
      ")",
      "SELECT *",
      "FROM   gaps",
      "WHERE  gap_end IS NOT NULL",
      "ORDER BY ${4:user_id_col}, gap_start;",
      "",
      "-- ─── Change Log ─────────────────────────────────────────────",
      "-- $CURRENT_YEAR-$CURRENT_MONTH-$CURRENT_DATE | Joseph P. Merten | Initial version",
      "${0}"
    ]
  }
}
